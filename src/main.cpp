#include <iostream>
#include <string>
#include <vector>

#include "kernel.h"

int main()
{
    int error;
    Kernel_t ksimOS;
    std::string buffer;
    std::vector<std::string> argv;
    return 0;
}
